#version 330 core

in vec2 TexCoord;

uniform vec3 Kd;
uniform sampler2D textureSampler;
uniform bool useTexture;

out vec4 FragColor;

void main()
{
	if(useTexture)
		FragColor = texture(textureSampler, TexCoord) * vec4(Kd, 1.0);
	else
		FragColor = vec4(Kd, 1.0);
}